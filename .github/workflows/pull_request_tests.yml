name: Unit tests
on:
  push:
    branches:
      - test

jobs:
  test-media:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: "Run collectstatic"
        run: |
            docker compose run test python manage.py collectstatic --noinput
        timeout-minutes: 5
